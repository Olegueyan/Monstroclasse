package net.olegueyan.monstroclasse.motor.core;

/**
 * Fix on Width and Height -> BOTH (keep the aspect)
 * <br>
 * For the squared node -> MAX (high size) MIN (low size)
 */
public enum FixingType
{
    BOTH,
    MAX,
    MIN
}